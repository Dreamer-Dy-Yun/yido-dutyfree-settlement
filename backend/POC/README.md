# POC

This folder contains local experiments and examples.

## Files

| Path | Responsibility |
| --- | --- |
| `redis_example.py` | Redis usage example/experiment. |
| `tttt.py` | Local scratch/experiment file. |

## Boundary Notes

- POC files are not part of the active API, DB, or worker contract.
- Move code into a production folder with tests before depending on it from runtime modules.
